import { SafeAreaView, Text } from "react-native";

export default function SuccessScreen() {
    return(
        <SafeAreaView >
            <Text style={{color:'#FF0000'}}>Respuesta de pago</Text>
        </SafeAreaView>
    );
    
};
